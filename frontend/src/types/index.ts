/** Re-export the retained REST API types from one stable module. */
export type {
  AddCommentRequest,
  Attachment,
  AttachmentParentKind,
  AuditEntry,
  CommentVisibility,
  CreateTicketRequest,
  ListTicketsParams,
  LoginRequest,
  PublicConfig,
  Ticket,
  TicketComment,
  TicketPriority,
  TicketStatus,
  UpdateTicketRequest,
  User,
  UserRole,
} from "./api";
